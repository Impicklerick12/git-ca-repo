## package.json: To install all packages
* How to create package.json?
```
npm.init
```
* Install express: npm i express
* Create script: start which runs the node app
* Require express and created a get route
* Start listening for server

* Install express: npm i nodemon
* Change script: nodemon which runs the node app with nodemon for restarting server
